/**
 * Reads and loads jsx to the wom
 * @param path Absolute path of the file
 */
export default function loadJSX(path: string) {
  if (/\.jsx$/.test(path)) {
    const moduleName = require.resolve(path);
    if (moduleName) delete require.cache[moduleName];
    require(path);
  }
}
