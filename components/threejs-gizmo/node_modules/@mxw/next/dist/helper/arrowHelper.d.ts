import * as THREE from '@mxw/three';
import { Color, ManualVisual, Section } from 'maxwhere';
export default class ArrowHelper {
    lineSection: Section;
    coneSection: Section;
    arrowObj: ManualVisual;
    color: Color;
    dir: THREE.Vector3;
    origin: THREE.Vector3;
    length: number;
    material_name: string;
    headLength: number;
    headWidth: number;
    constructor(dir: THREE.Vector3, origin: THREE.Vector3, length?: number, material_name?: string, color?: Color, headLength?: number, headWidth?: number);
    updateVisual(): void;
    private calculateSections;
    clear(): void;
}
